# Multi-Agent-Research-Assistant


## How to Run the project
Initialize uv, Create a venv and install packages
```
uv init
uv venv
source .venv/bin/activate
uv sync
```
Create an env file
`cp sample.env .env`

Edit The .env file and add your api key

Run the project
`python research.py`